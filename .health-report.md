# Codebase Health Report

**Date:** 2026-06-28
**Baseline:** first run (no prior baseline)

---

## Summary

| Metric                      | Value    | Trend | Notes                              |
| --------------------------- | -------- | ----- | ---------------------------------- |
| Clippy warnings             | 0        | →     | Clean — zero warnings at `-D warnings` |
| Tests passed                | 44 / 44  | →     | 100% pass rate, 0 skipped          |
| Tests failed                | 0        | →     |                                    |
| TODOs in source             | 2        | →     | See breakdown below                |
| Dep version inconsistencies | 0        | →     | All crates use workspace = true    |
| `cargo rail unify` opps     | 0        | →     | Workspace deps fully unified       |
| MSRV                        | 1.87.0   | →     |                                    |

---

## Clippy

**Result: PASS — 0 warnings**

```
cargo clippy --workspace --all-targets -- -D warnings
Finished `dev` profile in 0.20s
```

No regressions. No fix plan needed.

---

## Tests

**Result: PASS — 44/44**

```
44 tests run: 44 passed, 0 skipped
```

Breakdown by crate:

| Crate        | Tests | Pass | Fail | Skip |
| ------------ | ----- | ---- | ---- | ---- |
| agent-loop   | 23    | 23   | 0    | 0    |
| ail (unit)   | 10    | 10   | 0    | 0    |
| ail (demo)   | 11    | 11   | 0    | 0    |

No regressions. No fix plan needed.

---

## Dependency Ring / Depgraph

`cargo rail unify --check` reports:

```
Member edits: 0
Transitive pins: 0
no unification opportunities found
```

All workspace members use `dep = { workspace = true }` inheritance. No pinned
versions exist outside the root `[workspace.dependencies]` table. No ring
violations detected.

---

## TODO / FIXME Density

**2 real TODOs in source** (1 test-fixture string excluded from count)

| Location                               | Line | Content                                              | Action             |
| -------------------------------------- | ---- | ---------------------------------------------------- | ------------------ |
| `crates/ail/src/adapters/coursers.rs`  | 58   | `TODO(test/fuzz): fuzz target already implemented`   | Remove — done      |
| `crates/ail/src/phases/p5.rs`          | 50   | `TODO: fill in the exact JSON/TOML block to add`     | Intentional placeholder |

The `coursers.rs` TODO is stale — the fuzz target was implemented in
`fuzz/fuzz_targets/fuzz_parse_discover_output.rs`. **Fix: remove it (< 5 lines).**

---

## Workspace Dependency Consistency

All 3 crates (`agent-loop`, `ail`, `xtask`) inherit every dependency via
`workspace = true`. Crate versions are uniformly `0.1.0`. No inconsistencies.

---

## Regressions

None. This is the first baseline run.

---

## Fix Plan

### F1 — Remove stale fuzz TODO in `coursers.rs` (stale comment, ≤1 line)

- **File:** `crates/ail/src/adapters/coursers.rs:58`
- **Action:** Delete the comment line — the fuzz target exists and passes
- **Status:** Implementing immediately (< 20 lines)
